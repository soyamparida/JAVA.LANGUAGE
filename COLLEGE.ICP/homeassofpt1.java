package conceptCODES;
import java.util.Scanner;
import java.util.Random;
 
public class homeassofpt1 {

	public static void main(String[] args) {
		/*
		 Scanner scanner = new Scanner(System.in);

	        // Generate a random number (0 for scissor, 1 for rock, 2 for paper)
	        Random random = new Random();
	        int computerChoice = random.nextInt(3);

	        // Prompt the user to enter a number
	        System.out.print("Enter 0 for scissor, 1 for rock, or 2 for paper: ");
	        int userChoice = scanner.nextInt();

	        // Display the choices made by the user and the computer
	        System.out.println("You chose: " + choicegetuser(userChoice));
	        System.out.println("Computer chose: " + choicegetuser(computerChoice));

	        // Determine the winner
	        determineWinner(userChoice, computerChoice);

	        // Close the scanner
	        scanner.close();
	    }

	    // Helper method to get the choice as a string
	    private static String choicegetuser(int choice) {
	        switch (choice) {
	            case 0:
	                return "Scissor";
	            case 1:
	                return "Rock";
	            case 2:
	                return "Paper";
	            default:
	                return "Invalid choice";
	        }
	    }

	    // Helper method to determine the winner
	    private static void determineWinner(int userChoice, int computerChoice) {
	        if (userChoice == computerChoice) {
	            System.out.println("It is a draw ");
	        } else if ((userChoice == 0 && computerChoice == 2) ||
	                   (userChoice == 1 && computerChoice == 0) ||
	                   (userChoice == 2 && computerChoice == 1)) {
	            System.out.println("You won");
	        } else {
	            System.out.println("Computer wins!");
	        }
	        */
	        // q 3 
	    Scanner sc = new Scanner (System.in) ;
	    Random random = new Random();
	    int integer  = random.nextInt(12) + 1;
	    
	    // Display the corresponding month name 
	    //String monthname = getMonthName(integer);
	    //System.out.println("Randomly generated month : " + monthname);	
	   
	   // 	private static Stri
	    
	  
	   
	
	
	}
}
